-- 0011 — settings used by returns and support (empty until IXITEK writes its own policy text).
INSERT IGNORE INTO settings (setting_key, value_json) VALUES
 ('support.return_instructions', '""'),
 ('email.support_inbox', '""');
